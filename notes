git code

1. Unzip locally
# Go to the folder where the ZIP is downloaded
cd ~/Downloads   # or wherever it is

# Unzip
unzip llm-research-playbook.zip -d llm-research-playbook
cd llm-research-playbook

2. Create a new repo on GitHub

Go to GitHub → New Repository
.

Fill in:

Repo name: llm-research-playbook

Description: Roadmap + exercises for LLM research

Visibility: Public or Private (your choice).

Don’t initialize with README (you already have one).

Click Create Repository.

GitHub will show you the URL of the repo (e.g. https://github.com/yourusername/llm-research-playbook.git).

3. Initialize Git locally
# Inside the repo folder
git init
git branch -M main

# Add remote (replace with your GitHub URL)
git remote add origin https://github.com/yourusername/llm-research-playbook.git

# Add files and commit
git add .
git commit -m "Initial commit: LLM Research Playbook"

4. Push to GitHub
git push -u origin main